# Host port the status page is published on
PORT=8787

# How often to ping sessionserver.mojang.com
CHECK_INTERVAL_MINUTES=5

# Expose /api/trigger to run a check on demand (leave off in production)
ENABLE_TRIGGER=false
